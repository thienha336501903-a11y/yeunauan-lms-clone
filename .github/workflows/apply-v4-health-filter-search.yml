name: Apply V4 health filter search

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch:
    if: github.event.pull_request.head.ref == 'feat/v4-health-filter-search-20260820'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/v4-health-filter-search-20260820
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - name: Apply health filter/search patch
        run: node scripts/apply-v4-health-filter-search.mjs
      - name: Validate
        run: |
          npm test
          node --check utils/lms-handlers/admin-v4-source.js
      - name: Commit functional changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add v4-admin.html test/v4-source-admin.test.js
          git commit -m "feat: filter and search V4 health dashboard"
      - name: Remove one-time tooling
        run: |
          rm -f scripts/apply-v4-health-filter-search.mjs .github/workflows/apply-v4-health-filter-search.yml
          git add -A
          git commit -m "chore: remove V4 health filter tooling"
      - name: Push
        run: git push origin HEAD:feat/v4-health-filter-search-20260820
