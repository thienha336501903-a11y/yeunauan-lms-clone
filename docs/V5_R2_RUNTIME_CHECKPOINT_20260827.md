# V5 R2 runtime checkpoint — 2026-08-27

This checkpoint exists to trigger a fresh Preview deployment after Cloudflare R2 credentials were added to Vercel environments.

Expected runtime configuration names:

- `R2_ACCOUNT_ID`
- `R2_ACCESS_KEY_ID`
- `R2_SECRET_ACCESS_KEY`
- `R2_BUCKET=yeubep-v5-media-prod`

The V5 branch remains a parallel candidate. This file contains no secrets and does not enable V5 Production routing by itself.
